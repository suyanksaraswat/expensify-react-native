import React from 'react';
import {Text} from 'react-native';
import ScreenWraper from '../components/ScreenWraper';

function LoginScreen() {
  return (
    <ScreenWraper>
      <Text className="text-[#000]">Login Screen</Text>
    </ScreenWraper>
  );
}

export default LoginScreen;
